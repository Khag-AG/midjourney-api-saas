# Database (Railway provides this automatically)
DATABASE_URL=postgresql://user:password@host:port/database

# Server configuration
PORT=8080
NODE_ENV=production

# Data migration (optional, only for migration from file system)
# DATA_PATH=/path/to/old/data/folder

# Note: Discord credentials are now stored per user in the database
# Users provide their own Discord data when creating accounts